#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main()
{

	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);

	int arr[] = {5 , 4 , 2, 6, 7 , 7, 7, 8, 1};
// to rotate the array
	int size = sizeof(arr) / sizeof(int);
	// give the index from where you want to rotate
	rotate(arr , arr + 5 , arr + size);

	for (int i = 0 ; i < size ; i++)
		cout << arr[i] << " " ;



}
